services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=autobl...et
      - DATABASE_URL=sqlite:///data/autoblog.db
    volumes:
      - autoblog-data:/app/data

volumes:
  autoblog-data:
